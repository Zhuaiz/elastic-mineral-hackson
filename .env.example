# 复现本 solution 需要的环境变量。cp .env.example .env 后填入你自己的值。
#
# tp run 只用到 ES_URL + ES_API_KEY（作答走 ES 推理端点，检索走 ES）。
# 其余变量仅在从零重建语料（embed→index）或跑 demo 时需要。

# --- 必需：Elasticsearch（检索 + qwen 作答推理端点都在这个集群上）---
# 本项目公开了一个托管 ES 集群供任何人复现榜单。端点如下：
ES_URL=http://es-cn-jfq4v6dhq0001bxo7.public.elasticsearch.aliyuncs.com:9200

# 复现用的 API key：一个「只读 minerals-* 索引 + 只能调 qwen 推理端点」的限权 key。
# ⚠️ 这里绝不要放超管 key。生成限权 key 的命令见 README「公开复现」章节。
ES_API_KEY=<paste-the-read-only-reproduction-key-here>

# 或用 user/password（不推荐对外公开，改用上面的限权 API key）：
# ES_USER=
# ES_PASSWORD=

# --- 仅重建语料时需要（tp run 不需要）---
# 图像嵌入 / 文本推理端点用的 Jina key（jina-clip-v2）
JINA_API_KEY=
# 原始矿物属性抓取（Mindat）
MINDAT_API_KEY=
