# 复现本 solution 需要的环境变量。cp .env.example .env 后填入你自己的值。
#
# tp run 只用到 ES_URL + ES_USER/ES_PASSWORD（作答走 ES 推理端点，检索走 ES）。
# 其余变量仅在从零重建语料（embed→index）或跑 demo 时需要。

# --- 必需：Elasticsearch（检索 + qwen 作答推理端点都在这个集群上）---
# 需要一个装了 alibabacloud-ai-search 推理服务（qwen-plus 等 completion 端点）
# 且已灌好 minerals-images / minerals-species 索引的 ES 集群。
# 榜上那批 run 用的是阿里云托管 ES（私有）；换成你自己的集群同样能跑，
# 前提是按 scripts/ 重建索引 + 建同名推理端点。
ES_URL=http://your-es-host:9200
ES_USER=elastic
ES_PASSWORD=changeme
# 或用 API key 替代 user/password：
# ES_API_KEY=

# --- 仅重建语料时需要（tp run 不需要）---
# 图像嵌入 / 文本推理端点用的 Jina key（jina-clip-v2）
JINA_API_KEY=
# 原始矿物属性抓取（Mindat）
MINDAT_API_KEY=
